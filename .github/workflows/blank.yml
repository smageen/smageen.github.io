name: Import Cydia DEBs

on:
  workflow_dispatch:

jobs:
  import-debs:
    runs-on: ubuntu-latest
    timeout-minutes: 600  # ⏱ 10 часов
    strategy:
      matrix:
        part: [part1.zip, part2.zip, part3.zip, part4.zip, part5.zip]

    steps:
    - name: Проверка диска
      run: df -h

    - name: Установка pv (для прогресса)
      run: sudo apt-get install -y pv unzip

    - name: Скачивание архива ${{ matrix.part }}
      run: |
        echo "🔽 Загрузка ${{ matrix.part }}..."
        wget -c https://archive.org/download/cydia_repository_archive/${{ matrix.part }} -O ${{ matrix.part }}
        echo "✅ Загрузка завершена."

    - name: Распаковка с прогрессом
      run: |
        echo "📦 Распаковка ${{ matrix.part }}..."
        pv ${{ matrix.part }} | unzip -o -d debs_${{ matrix.part }} -

    - name: Логирование файлов
      run: |
        echo "📄 Список файлов в ${{ matrix.part }}:"
        find debs_${{ matrix.part }}/ -type f -name "*.deb" | tee debs_list_${{ matrix.part }}.txt

    - name: Загрузка артефакта
      uses: actions/upload-artifact@v4
      with:
        name: debs-${{ matrix.part }}
        path: debs_${{ matrix.part }}/
